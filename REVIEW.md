# 东林戒律 RAG 系统 · 复盘与优化记录

> 本文档用于持续记录系统建设过程中遇到的问题、解决方案、优化方向及原因价值。  
> 每次较大改动后，可在对应章节追加条目，保持项目演进的完整脉络，也方便后续写博客时直接取材。

---

## 目录

1. [项目背景](#1-项目背景)
2. [整体演进时间线](#2-整体演进时间线)
3. [问题与解决方案复盘](#3-问题与解决方案复盘)
   - 3.0 [早期架构搭建（昨天之前）](#30-早期架构搭建昨天之前)
   - 3.1 [导入与数据层](#31-导入与数据层)
   - 3.2 [检索与生成层](#32-检索与生成层)
   - 3.3 [输出与规范层](#33-输出与规范层)
   - 3.4 [测试与工程化](#34-测试与工程化)
4. [已完成的系统优化](#4-已完成的系统优化)
5. [已实现的进一步细化方向](#5-已实现的进一步细化方向)
6. [更长期的探索方向](#6-更长期的探索方向)
7. [附录：关键设计决策](#7-附录关键设计决策)
8. [更新记录](#8-更新记录)

---

## 1. 项目背景

本项目是一个面向佛教戒律领域的 RAG（Retrieval-Augmented Generation）问答系统。核心定位是：

- **知识库为主**：严格依据结构化戒律数据回答。
- **权威经典兜底**：知识库未覆盖时，有限度引用公认佛教经典和祖师大德开示。
- **身份聚焦**：根据用户身份（居士戒 / 沙弥戒 / 比丘戒）过滤检索范围，避免跨身份混淆。

---

## 2. 整体演进时间线

| 阶段 | 时间 | 核心内容 |
|------|------|----------|
| 项目立项 | 早期 | 确定佛教戒律 RAG 方向：知识库为主 + 权威经典兜底 + 身份聚焦 |
| 基础架构搭建 | 早期 | 选型 LangChain + Chroma + HuggingFace 本地嵌入 + DeepSeek API；建立 `rag/` 模块结构 |
| 数据结构化 | 早期 | 从 `data/jielv.txt` 纯文本迁移到 `knowledge_base.json`，引入 role / domain / source / category / severity 元数据 |
| 分层建库 | 早期 | 单一 Chroma 库拆分为比丘戒 / 沙弥戒 / 居士戒三层独立向量库，杜绝跨身份混淆 |
| 来源与规范 | 早期 | SYSTEM_PROMPT 强制真实经文出处；`cli.py` 追加免责声明；`format_context()` 注入 `source` 字段 |
| 身份聚焦 | 早期 | `generate()` 增加 `role` 参数；Prompt 增加身份聚焦规则；检索按身份过滤 |
| 数据迁移 | 近期 | `knowledge_base.json` 移至 `data/` 目录 |
| 回答质量优化 | 近期 | 软兜底机制、severity 等级标注、空检索兜底、跨身份回答控制 |
| 体验优化 | 近期 | loading 效果、Gradio 多轮对话、对话状态管理 |
| 工程化加固 | 近期 | 日志反馈、限流、重试、Schema 校验、测试集、JSON 结构化输出、PDF 智能分块 |

---

## 3. 问题与解决方案复盘

### 3.0 早期架构搭建（昨天之前）

| 问题 | 解决方案 | 原因 / 好处 |
|------|----------|-------------|
| 知识库使用纯文本 `jielv.txt`，无结构化元数据 | 设计并迁移到 `knowledge_base.json`，每条记录包含 `role`、`domain`、`source`、`category`、`severity` | 便于身份过滤、来源标注、等级区分和后续扩展 |
| 单一向量库导致身份混淆 | 按身份拆分三个独立 Chroma 库：`jie_lv`（比丘戒）、`sha_mi`（沙弥戒）、`upasaka`（居士戒） | 物理隔离不同身份数据，从根本上避免跨身份检索 |
| 检索无 domain 过滤，非戒律问题也硬凑答案 | 增加 `domain="jielv"` 元数据过滤和相关度阈值 `MIN_RELEVANCE=0.50` | 防止无关问题得到牵强回答，提升回答可信度 |
| 回答出处标注为行号，不真实 | `SYSTEM_PROMPT` 强制要求引用真实经文/法师著作；`format_context()` 注入 JSON 中的 `source` 字段 | 避免编造出处，增强用户信任 |
| CLI 输出缺少免责声明 | `cli.py` 自动生成并追加免责声明「内容仅供参考，以丛林规约为准」 | 符合宗教/法律内容的谨慎性要求 |
| 回答缺少戒律等级标识 | 在 `knowledge_base.json` 中引入 `severity` 字段；`loader.py` 透传 metadata；Prompt 要求按等级标注 | 让用户快速识别戒条重要性 |
| 跨身份回答发散 | `generate()` 增加 `role` 参数；`SYSTEM_PROMPT` 增加身份聚焦规则；检索和生成均按身份隔离 | 避免把比丘内容答给居士，或反之 |
| `rag/` 模块文件缺少职责说明 | 为 `__init__.py`、`loader.py`、`retriever.py`、`vector_store.py`、`generator.py` 添加顶层 docstring | 提升代码可维护性，便于新人理解 |

### 3.1 导入与数据层

| 问题 | 解决方案 | 原因 / 好处 |
|------|----------|-------------|
| `ingest.py` 写死 PDF 文件名 | 改为命令行传参，默认 `data/jielv.pdf` | 导入脚本不再依赖固定文件名，换 PDF 不需要重命名 |
| PDF → JSON → 向量库链路不通 | `pdf_loader.py` 补全 `domain` / `severity` 字段；`ingest.py` 重写为命令行传参 + 清空旧库 + 按身份分组建库 | 保证导入数据与 `knowledge_base.json` schema 一致，重建向量库不会残留过期数据 |
| `knowledge_base.json` 迁移到 `data/` 目录 | `loader.py`、`init_db.py`、`ingest.py` 统一读取 `data/knowledge_base.json` | 数据与代码分离，项目结构更清晰，后续部署也更容易 |
| 知识库覆盖不足 | 条目从 25 条扩展到 42 条，新增比丘戒/沙弥戒核心条目（非时食、三衣、金银、歌舞观听、沙弥十戒等） | 提升常见戒律问题的命中率，减少"明明有答案却返回未找到"的情况 |
| PDF 导入无校验、无合并能力 | 新增 Schema 校验（`validate_records`）和 `--merge` 合并去重模式 | 防止字段缺失导致建库失败；支持增量补充知识库而不重复 |
| 检索展示内容被截断（如"《敕修百丈清"） | 从 `data/jielv.txt` 重建知识库：按行内【身份】标签拆分，长段落按句末标点切分；overview 内容标记为 `通用` 写入所有身份库；`ingest.py` 支持 `.txt` 导入 | 保证每条检索原文都是完整句子；通用内容可被所有身份检索，提升覆盖率 |

### 3.2 检索与生成层

| 问题 | 解决方案 | 原因 / 好处 |
|------|----------|-------------|
| 跨身份回答发散 | `generate()` 增加 `role` 参数；`SYSTEM_PROMPT` 增加"当前用户身份为 {role}"规则；按身份分层建库 + `domain` 过滤 | 避免把比丘内容答给居士，或反之；回答范围严格聚焦 |
| 检索不相关时硬拒答 | 增加 `is_retrieval_relevant()` 校验；不相关时清空误导性 docs，走权威经典软兜底 | 从"答得太少"改为"谨慎但有用"，兼顾准确性与覆盖率 |
| 空检索时不兜底 | `web_app.py` / `cli.py` 在指定身份但 docs 为空时，调用 `generate(question, [], role=role)` | 知识库未覆盖的合理戒律问题（如比丘穿衣）也能得到权威回答 |
| 首次提问无 loading 效果 | 原方案增加 `loading_status` 组件和事件链；后升级为 `gr.ChatInterface`，自带加载状态 | 用户点击后立即有反馈，避免误以为系统无响应 |
| 回答格式不稳定 | `generator.py` 增加格式校验 `_check_format()`，缺少 `【答】`/`【依据】` 时自动二次请求 | 保证输出结构统一，便于用户阅读和后续解析 |
| API 偶发失败 | `_call_llm()` 增加失败重试（最多 2 次，指数退避） | 提升服务稳定性，减少单次网络抖动导致的失败 |
| 上下文过长 | 增加 `_truncate_context()`，超过 3000 字自动截断 | 避免超长请求导致费用增加或模型处理异常 |
| 语义检索"换个词就搜不到" | 语义无结果时 fallback 到关键词字符匹配 | 在零成本新增依赖的前提下提升召回率 |
| 检索结果重复展示 | `retriever.py` 返回前按 `(content, source)` 去重 | 解决 role="不限" 跨库检索时同一原文重复出现的问题 |
| 关键词召回噪声导致排序变差 | 同义词每个 key 只触发一次并降权到 0.5；忽略单字匹配；语义结果足够时优先语义，不足时才 RRF 融合关键词 | 避免"五戒""居士"等通用词权重过高，提升检索相关性 |
| 多轮对话 `history` 格式报错 | `conversation.py` 新增 `_parse_history_item()`，兼容 tuple/list/dict/ChatMessage 三种历史格式 | 适配 Gradio 6.0 新的 history 数据结构，避免 `too many values to unpack` |
| 身份下拉框第二轮不生效 | `web_app.py` 身份更新逻辑从 `turn_count == 1` 改为用户每次显式选择身份都更新 `state.current_role` | 保证多轮对话中切换身份后立即按新身份检索 |

### 3.3 输出与规范层

| 问题 | 解决方案 | 原因 / 好处 |
|------|----------|-------------|
| 回答缺少戒律等级标识 | `knowledge_base.json` 增加 `severity` 字段；`loader.py` 透传 metadata；`SYSTEM_PROMPT` 要求按 `severity` 标注等级 | 让用户一眼识别戒条重要性（根本戒 vs 遮戒） |
| 来源标注不规范 | `SYSTEM_PROMPT` 强制要求区分"知识库来源"和"补充来源"；`format_context()` 注入真实 `source` | 避免编造出处，增强可信度 |
| 越界问题硬答 | `SYSTEM_PROMPT` 增加越界处理规则，非戒律问题直接拒答 | 防止从戒律资料中牵强附会，保护系统边界 |

### 3.4 测试与工程化

| 内容 | 说明 |
|------|------|
| 创建 `tests/test_qa.py` | 8 个检索用例 + 5 个生成用例 + 2 个拒答用例 |
| 检索测试 8/8 通过 | 验证身份分层、domain 过滤、相关度阈值、关键词 fallback 均按预期工作 |
| 更新 `requirements.txt` | 新增 `gradio`、`langchain-chroma`、`pypdf`；移除未使用的 `langchain-community`、`langchain-text-splitters` |
| 生产化安全配置 | Web 服务默认绑定 `127.0.0.1`；增加问题长度限制（500 字）和全局限流（30 次/分钟） |
| Gradio 6.0 兼容性修复 | `ChatInterface` 的 `examples` 改为 list-of-lists 格式；`theme` 参数从 `gr.Blocks()` 移到 `demo.launch()` | 适配 Gradio 6.0 API 变化，保证 `python web_app.py` 正常启动 |
| Web UI 移除内置 Examples | 直接移除 `ChatInterface` 的 `examples` 参数，避免表格中出现"访问令牌"列 | 防止前端暴露/显示访问令牌字段，提升交互体验 |

---

## 4. 已完成的系统优化

以下 8 项为系统评估后逐步落地的优化（前 2 项为早期完成，后 6 项为最新完成）：

| 序号 | 优化项 | 实现内容 | 价值 |
|------|--------|----------|------|
| 1 | 扩充知识库 | 条目从 25 条扩展到 42 条，覆盖比丘戒、沙弥戒、居士戒核心场景 | 提升常见问题的直接命中率 |
| 2 | 建立测试集 | `tests/test_qa.py` 覆盖检索召回、端到端生成、拒答场景 | 后续改动可快速回归验证 |
| 3 | 检索器增强 | 语义检索 + 同义词扩展 + 关键词匹配 + RRF 重排融合 | 解决"换个词就搜不到"的问题，提高召回率 |
| 4 | 多轮对话 | `gr.ChatInterface` + `rag/conversation.py` 状态管理，支持身份切换检测和追问消歧 | 支持追问和指代消解，交互更自然 |
| 5 | 日志与反馈 | `rag/logger.py` 自动记录 Q&A；差评自动归入 `data/bad_cases.json`；支持日志分析报表 | 形成 bad case 闭环，为迭代提供数据基础 |
| 6 | 生成可控性 | API 失败重试、格式校验、上下文截断、详细程度选择、JSON 结构化输出、置信度标识 | 提升稳定性、输出一致性和可解析性 |
| 7 | 生产化安全 | 默认本机访问、输入长度校验、全局 + 单会话限流、输入审核、可选 ACCESS_TOKEN 访问控制 | 降低滥用风险，适合本地/小范围部署 |
| 8 | PDF 导入优化 | Schema 校验、`--merge` 合并去重、覆盖前自动备份、智能语义分块、`--preview` 预览 | 数据导入更健壮，支持持续扩充 |

---

## 5. 已实现的进一步细化方向

以下 6 个方向已按预期落地：

### 5.1 检索器增强：同义词扩展 + RRF 重排

**实现内容：**
- `rag/retriever.py` 新增同义词扩展表（如"吃饭→食、非时食、持午"、"衣服→三衣、袈裟"）。
- 关键词召回从简单字符命中升级为带权重的多词命中评分。
- 语义结果与关键词结果通过 Reciprocal Rank Fusion（RRF）融合排序。

**价值：** 召回率进一步提升，尤其适合口语化、同义不同字的提问。

### 5.2 日志反馈闭环：自动化 bad case 挖掘

**实现内容：**
- `rag/logger.py` 扩展 `analyze_logs()` 和 `print_report()`，可统计总问答数、兜底率、空检索率、差评率、高频未覆盖问题 TOP10。
- 用户提交"无帮助 / 部分正确"反馈时，自动追加到 `data/bad_cases.json`。
- 命令行运行 `python -m rag.logger` 即可输出日志分析报告。

**价值：** 形成"提问 → 反馈 → 分析 → 补库 → 验证"的数据闭环。

### 5.3 多轮对话：对话状态管理 + 追问消歧

**实现内容：**
- 新增 `rag/conversation.py`，定义 `ConversationState` 类，记录当前身份、主题、兜底次数、轮次、请求时间等。
- 实现 `detect_role_switch()` 检测身份切换（如"那比丘呢？"）。
- 实现 `is_topic_switch()` 和 `detect_follow_up_topic()` 区分追问与话题切换。
- `web_app.py` 使用 `gr.State` 传递状态，回答时基于状态构造上下文，而非简单拼接历史。

**价值：** 追问更自然，减少跨身份/跨话题的答非所问。

### 5.4 生成可控性：JSON 结构化输出 + 详细程度 + 置信度

**实现内容：**
- `rag/generator.py` 新增 `_DETAIL_PROMPT`，支持"简洁 / 标准 / 详细"三种回答长度。
- 新增 `JSON_SYSTEM_PROMPT`，支持输出结构化 JSON（answer / sources / severity / confidence / note）。
- 根据检索情况自动判定置信度（high / medium / low），并在 Markdown 输出末尾标注。
- `web_app.py` 增加"回答详细程度"下拉框和"结构化输出（JSON）"复选框。

**价值：** 回答更可调、更可解析，便于前端渲染和后续审核。

### 5.5 生产化安全：多层限流 + 输入审核 + 访问控制

**实现内容：**
- 全局每分钟 30 次 + 单会话每分钟 10 次双重限流。
- 输入审核：拦截明显非戒律/敏感关键词（如股票、赌博、政治等）。
- 访问控制：通过环境变量 `ACCESS_TOKEN` 开启令牌校验，未配置则不启用。
- `web_app.py` 中令牌输入框仅在配置了 `ACCESS_TOKEN` 时显示。

**价值：** 更适合对外或小范围部署，降低滥用和 API 费用风险。

### 5.6 PDF 导入优化：智能语义分块 + 元数据提取 + 预览

**实现内容：**
- `rag/pdf_loader.py` 新增 `_semantic_chunk()`，按戒条编号、标题、【】标签、空行等语义边界切分大段内容。
- 自动从分块开头提取"第 X 条"、"（一）"等编号，合并到 `source` 字段。
- `load_pdf_to_json()` 增加 `smart_chunk` 和 `preview` 参数。
- `ingest.py` 新增 `--preview`（预览前 3 条）和 `--no-chunk`（关闭分块）命令行参数。

**价值：** 导入数据粒度更细、来源更准确，检索精度更高；预览模式避免误操作。

---

## 6. 更长期的探索方向

如果后续还想继续深入，可以考虑：

1. **引入 BM25 / jieba 专业分词**：当前同义词扩展基于字符串匹配，可进一步用 BM25 或专业分词提升关键词召回质量。
2. **A/B 测试框架**：对比不同 Prompt / 检索策略在真实日志上的表现。
3. **自动补库建议**：基于 bad_cases.json 和未覆盖问题，自动生成建议补充的知识条目。
4. **按 IP 限流**：当前单会话限流依赖 Gradio State，若需更严格防护，可结合 Gradio 的 `request` 对象做 IP 级限流。
5. **答案质量自动评估**：用另一个模型或规则对每条回答打分，减少人工反馈依赖。

---

## 7. 附录：关键设计决策

### 6.1 知识库为主 + 权威经典兜底

- **决策**：系统采用"知识库为主 + 权威经典兜底"的混合问答模式。
- **原因**：纯严格知识库模式覆盖面不足；完全开放模型自由回答编造风险过高。
- **结果**：知识库覆盖 80% 常见居士生活问题，剩余 20% 由权威经典补充，且必须在【依据】中区分两类来源。

### 6.2 身份聚焦与跨域拦截

- **决策**：按身份（比丘戒 / 沙弥戒 / 居士戒）建立独立向量库，检索时只查对应库，并通过 `domain="jielv"` 过滤。
- **原因**：避免用户选比丘戒时回答混入居士/沙弥内容，确保回答范围严格匹配用户身份。
- **结果**：跨身份回答发散问题得到解决。

### 6.3 软兜底而非硬拒答

- **决策**：当检索结果与问题不相关或为空时，清空误导性资料，允许模型基于权威经典谨慎回答，但仍紧扣当前身份。
- **原因**：知识库不全时，硬拒答会让用户觉得"系统没用"；完全放开又有编造风险。
- **结果**：在保持审慎的同时提升了覆盖率，用户反馈从"答得太少"转为可接受。

### 6.4 为什么选择 LangChain 而非 LlamaIndex

- **决策**：项目采用 LangChain（`langchain-chroma`、`langchain-huggingface`、`langchain-core`）构建 RAG 流水线，而不是 LlamaIndex。
- **原因**：
  - 当前系统是典型的"轻量 RAG"：加载 → 嵌入 → 向量检索 → 调用 LLM，LangChain 的抽象层级正好匹配，学习和维护成本更低。
  - LlamaIndex 更适合大规模、复杂文档索引、Agent 查询引擎、多跳推理等场景；本项目早期只有几十条戒律条文，使用 LlamaIndex 会显得过重。
  - LangChain 对 Chroma、HuggingFace 本地嵌入、自定义 Prompt / 状态管理 / 限流等需求的集成更直接，便于保留高度自定义的检索和生成逻辑。
- **结果**：以较少的概念和依赖完成了核心功能；若未来知识库扩展到成百上千条、需要高级检索策略（reranking、HyDE、层级索引）时，再评估迁移或混合使用 LlamaIndex。

---

## 更新记录

| 日期 | 更新人 | 内容摘要 |
|------|--------|----------|
| 2026-08-09 | AI 助手 | 首次整理：完成问题复盘、已优化项、待细化方向、关键设计决策 |
| 2026-08-09 | AI 助手 | 实现 6 个进一步细化方向：检索器增强、日志闭环、多轮状态管理、生成可控性、生产化安全、PDF 智能分块；同步更新文档 |
| 2026-08-09 | AI 助手 | 补充昨天之前的早期架构搭建内容，完善项目生命周期记录 |
| 2026-08-09 | AI 助手 | 修复检索内容截断问题：从 `jielv.txt` 重建知识库，按句末标点切分保证完整句子；修复关键词召回过度扩展问题；检索融合优先语义结果；`ingest.py` 支持 TXT 导入 |
| 2026-08-09 | AI 助手 | 在附录补充"LangChain 而非 LlamaIndex"的关键设计决策，记录选型原因与未来迁移考量 |
| 2026-08-09 | AI 助手 | 补充近期 Gradio 6.0 兼容性、多轮对话 history 解析、身份每轮生效、检索去重与语义优先融合等修复记录 |

---

*本文档持续更新中，后续每次较大改动后建议追加一条更新记录，并在对应章节补充内容。*
